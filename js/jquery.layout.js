(function($)
{

	/**
	 * layout plugin for jQuery
	 */
	$.fn.inputTitleHelper = function(options)
	{

		//------------------------------------------------------------------------------------ settings
		const settings = $.extend({
			help_class: 'helper'
		}, options)

		return this
	}

})( jQuery )
